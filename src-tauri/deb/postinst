#!/bin/bash
# postinst script for companion-chat

# Create symlink for CLI mode
ln -sf /usr/bin/companion-chat /usr/bin/companion-chat-cli

echo "Companion Chat installed successfully!"
echo "- GUI mode: companion-chat"
echo "- CLI mode: companion-chat-cli (or: companion-chat chat)"
